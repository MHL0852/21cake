import * as Types from "../action-types";

let initState = {
  poricShop:[{id:0,num:0}]
};

function detail(state = initState, action) {
  switch (action.type) {

  }
  return state;
}

export default detail;