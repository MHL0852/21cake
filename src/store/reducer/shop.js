import * as Types from "../action-types";

let initState = {
  poricShop:[{
    id:'0',
    goodsSizeId:'0',
    num:0
  }]
};

function shop(state = initState, action) {
  switch (action.type) {

  }
  return state;
}
export default shop;